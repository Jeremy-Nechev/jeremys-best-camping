source "https://rubygems.org"

# Pins the same Jekyll + plugin versions GitHub Pages builds with remotely, so
# `bundle exec jekyll serve` behaves the same locally. Requires Ruby 2.7+.
gem "github-pages", group: :jekyll_plugins
